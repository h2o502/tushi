# 图示 tushi — SkillHub 发布文稿

> 本文件为 skillhub.cn 发布用文案：技能介绍（上架页正文）+ 版本说明。
> 英文名/标签：tushi

---

## 技能介绍（上架页正文）

### 一句话

把复杂逻辑画成给人看的交互图：AI 写 PlantUML → 确定性引擎渲染 → 单文件交互式 HTML，交付一个浏览器打开即用的地址。

### 解决什么问题

需要把产品架构、知识体系、复杂流程「画出来给人看」时，常见做法有三种，各有硬伤：

- **AI 直出图片**：LLM 排版能力弱，字挤线穿，同一输入两次结果不一样
- **手绘/白板工具**：好看，但改一次图重画一次，无法随代码库演进
- **静态架构图**：图能看，但读者只能看，点不了、问不了

图示 tushi 的做法是让每个环节只干自己最强的事：

| 环节 | 谁来干 | 为什么 |
|---|---|---|
| 内容理解 → PlantUML DSL | LLM | puml 是训练语料充分的成熟 DSL，母语级，一次成型 |
| DSL → SVG 排版 | PlantUML 引擎 | 二十年打磨的确定性渲染，同输入同输出，可复现 |
| SVG → 交互层 | 自研 viewer | 热区 + 白话浮层 + 缩放，纯前端零依赖 |

### 工作方式（AI 侧五步）

1. **理解内容**：读用户给的文档/代码/描述，选图类型（时序/组件/状态/活动/类/ER/部署/思维导图）
2. **写 puml**：AI 写 PlantUML 源码，不改写不发明，只做结构化映射
3. **写 notes**：AI 同时写白话注释 sidecar——锚点是图中的参与者/节点名，正文是给人看的大白话
4. **渲染**：`tushi.py render --puml x.puml --notes x.notes.json` → SVG → 拼装单文件 index.html
5. **交付**：给用户 index.html（本地双击打开 / 部署成 URL 均可）

渲染失败时 PlantUML 报错自动喂回 LLM 修正，最多重试 3 轮。

内置「渲染陷阱踩坑清单」：泳道与 partition 禁止混用、footnote 与泳道不兼容、渲染后必须做文本级完整性校验——沉淀自实战事故，渲染器不报错但产出错误布局的坑一次避开。

### 交付物

**一个单文件 index.html**：内嵌 SVG 底图 + 白话注释数据 + 交互引擎。

- 零网络请求、零外部依赖，发给谁都能打开
- 读者点击图中任意元素，弹出这个组件「是什么/为什么存在/关键决策」的白话解释
- 工具栏缩放（默认原始宽度自由画布，左右滚动）+ 高亮点位
- 源文件 .puml 是纯文本：git 可 diff，手改一行重渲染，图与知识库共同演进

### 适用场景

- 「画个架构图」「把这个流程可视化」「给用户讲清楚这个系统」
- 交接文档、评审材料、新人 onboarding、周报汇报
- 任何需要把复杂逻辑变成**别人能看懂、还能点着问**的图的时刻

### 依赖

- Java 运行时（JRE 11+）
- PlantUML jar（skill 自带 vendor/plantuml.jar，或 `PLANTUML_JAR` 环境变量指定）
- Python 3.8+（仅标准库）

---

## 版本说明

### v1.2.0（2026-09-02）

**修复**

- **交付方式根因修复**：AI 平台执行渲染后误启动 HTTP 服务、给 localhost/内网 IP 地址导致用户打不开。根因为 `tushi.py` 输出「或部署后给 URL」+ `SKILL.md` 写「访问路径」两处措辞误导。三处收口：
  - `tushi.py` 渲染完成输出改为「单文件零依赖，本地双击打开即可。禁止启动 HTTP 服务。」
  - `SKILL.md` 工作方式第 4 步改为明确交付文件路径 + 禁止 HTTP 服务/localhost/内网 IP
  - `SKILL.md` 新增「交付方式（强制）」小节，6 条硬规则封死「起服务→给 URL」路径

### v1.1.0（2026-09-02）

**新增**

- `SKILL.md` 新增「渲染陷阱（必读踩坑清单）」章节，沉淀三条实战踩坑规则（渲染器不报错但产出错误布局）：
  - **泳道与 partition 禁止混用**：切换泳道后的内容全包在 partition 里时，partition 块会被画到其他泳道或悬浮错位，被切走的泳道只剩空标题（渲染成功、无报错，极易漏发现）。正确做法是纯泳道或纯分区二选一，归属感写进 partition 标题。来源：L0+L1 全景图重构事故（2026-09-02），smartRoute 泳道整块丢失
  - **footnote 与泳道不兼容**：泳道活动图里写 footnote 会触发 Syntax Error。注释信息放 notes.json 白话层，不塞图内
  - **渲染后必须做文本级完整性校验**：提取 SVG 全部 `<text>`（PlantUML 输出是数字字符引用，需 `html.unescape` 解码后比对），确认关键节点名全部存在、无空泳道标题残留。渲染"成功"≠内容完整

**修复**

- 修复 `SKILL.md` 中 `description` 字段包含 Markdown 语法的问题，改为纯文本触发说明，解决 SkillHub 页面 Markdown 符号直接暴露、排版混乱的问题
- 将原本挤在 `description` 里的详细说明（核心能力、工作方式、notes 格式、交付物、依赖、触发场景）迁移到 `SKILL.md` 正文
- 补全 `SKILL.md` 正文，解决 SkillHub 概述页"无法加载文档"的问题
- 精简 YAML frontmatter，保留 `slug` / `name` / `displayName` / `description` / `version` / `license` 必需字段

### v1.0.0（首发）

**核心管线**

- PlantUML → SVG → 单文件交互式 HTML 完整链路
- `tushi.py render` CLI：puml + notes sidecar → 交付物 index.html
- 渲染失败报错自动喂回重试（默认 3 轮，`PUML_RETRY_ROUNDS` 可调）

**交互引擎（puml-viewer.js，约 15KB）**

- 热区自动绑定：遍历 SVG `<text>` 按 notes 锚点匹配（exact / prefix / contains / regex）
- 白话浮层：JS 计算定位，下方优先、越界翻转、视口夹紧
- 工具栏：缩放（±/适应/原始宽度）、热区高亮
- 三种接入模式：URL 参数（`?src=&notes=`）、`data-puml` 属性内嵌、**单文件交付**（内嵌数据零请求）
- 零业务文案，任何 PlantUML 图均可复用

**工程细节**

- 中文支持：Noto Sans CJK SC 字体注入 + UTF-8 全链路
- 内联安全：`</script>` 字面量转义，防止内嵌内容提前终止脚本块
- 源码自包含：.puml / .svg / notes.json 随交付物同目录归档，可追溯可重渲染

**已知边界**

- 单张图参与者建议 ≤12 个（超了 AI 会自动分主次，细节进 notes）
- 图内交互依赖 SVG 的 `<text>` 锚点（PlantUML 输出天然满足）

### 路线图（候选）

- v1.1：多图管理（一个主题拆多张图，带导航）
- v1.2：notes 浮层支持图片/链接引用
- v1.x：部署形态（一键起静态服务，交付 URL 而非文件）

---

## 标签建议

`tushi` `图示` `PlantUML` `架构图` `可视化` `交互图` `时序图` `流程图` `知识表达`
